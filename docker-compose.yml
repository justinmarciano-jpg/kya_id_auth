services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: kya_id_auth
      POSTGRES_USER: kya
      POSTGRES_PASSWORD: kya_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
